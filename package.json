{
  "name": "mailslurp-app",
  "version": "1.0.0",
  "description": "Static mail application using MailSlurp API",
  "scripts": {
    "build": "node build-config.js",
    "postinstall": "node build-config.js"
  },
  "engines": {
    "node": ">=14.x"
  }
}

